---
title: "J01 · Changer d'outil sans changer de métier : les fondations — démonstration formateur"
subtitle: "Atelier IFORD × GDSG 2026 · Lundi 27 juillet 2026 · Référents : M. Teda, R. Dzita"
author: "Geospatial Data Science Group — IFORD"
format:
  html:
    toc: true
    toc-depth: 2
    toc-location: left
    number-sections: false
    code-tools: true
    code-fold: false
    df-print: paged
    theme:
      - cosmo
      - ../_commons/styles/demo_report.scss
execute:
  eval: true       # exécute le code ET affiche les sorties
  echo: true
  error: true      # un chunk en erreur n'interrompt pas le rendu
  warning: true    # PHASE DE TEST : avertissements visibles (repasser a false
                   # pour la version distribuee aux participants si trop bavard)
  message: true
  fig-width: 8
  fig-height: 5
---

::: {.callout-note}
## Comment lire ce document

Ce rapport est le **miroir exécuté** de `script_formateur_J01.R` : chaque section montre
le code **et sa sortie**. Ce dossier est **autonome** : ouvrez-le directement dans RStudio
(ou double-cliquez le `.qmd`). Les données sont dans le sous-dossier `datasets/` et les
sorties vont dans `outputs/` — **tous les chemins sont relatifs à ce dossier**.

1. Une fois : `source("install_packages_day.R")` (packages du jour).
2. Vérifier que `datasets/` contient bien les fichiers listés dans le README du jour.
3. Cliquer **Render**. Les imports protégés par `file.exists()` laissent le rendu aboutir
   même si une donnée manque (la sortie concernée affiche alors un message).
:::

```{r}
#| label: setup
#| include: false
dir.create("outputs", recursive = TRUE, showWarnings = FALSE)
```

## 0. Vérifications de départ

```{r}
#| label: chunk-01
##    (equivalent : verifier sa version de STATA/SPSS avant de commencer)

# Version de R utilisee -- a comparer avec celle installee en salle
R.version.string

# Repertoire de travail courant (equivalent du "cd" en STATA)
getwd()

# --- Astuce : travailler par PROJET RSTUDIO --------------------------------
# File > New Project > Existing/New Directory
# Un projet RStudio associe automatiquement un dossier de travail au script,
# ce qui evite les chemins absolus qui cassent d'un ordinateur a l'autre.
# Une fois le projet ouvert, getwd() renvoie directement la racine du projet.
```

## 1. De STATA / SPSS à R : tableau de correspondance

À garder sous les yeux toute la journée.

| Notion | STATA / SPSS | R |
|---|---|---|
| Éditeur de commandes | Do-file editor / Syntax editor | Script editor (RStudio) |
| Fenêtre de résultats | Results window | Console |
| Extensions | ado-files, packages SPSS | Packages CRAN |
| Fichier de données natif | `.dta` / `.sav` | `haven::read_dta()` / `read_sav()` |
| Assignation d'une valeur | `generate` / `gen` ; `compute` | `<-` |
| Structure du jeu de données | `describe` / `codebook` | `str(df)` / `glimpse(df)` |
| Premières lignes | `browse` / Data View | `head(df)` |
| Statistiques descriptives | `summarize` | `summary(df)` |
| Tri à plat | `tab varname` | `table(df$varname)` |
| Valeurs manquantes | `misstable summarize` | `colSums(is.na(df))` |
| Commentaire | `*` ou `//` | `#` |

## 2. R et RStudio : deux choses différentes

**R** est le langage et le moteur de calcul — l'équivalent du moteur STATA, qui
tourne même sans interface graphique. C'est lui qu'on installe en premier.

**RStudio** est l'environnement de travail (IDE) qui se connecte automatiquement
à R après son installation. Il s'organise en quatre panneaux :

| Panneau | Position | Rôle |
|---|---|---|
| Script editor | haut-gauche | écrire et conserver le code |
| Console | bas-gauche | exécuter, voir les résultats |
| Environment / History | haut-droite | les objets créés, l'historique |
| Files / Plots / Packages / Help | bas-droite | fichiers, graphiques, aide |

### Installation (slide 6)

1. **R** → [cran.r-project.org](https://cran.r-project.org)
2. **RStudio** → [posit.co/download/rstudio-desktop](https://posit.co/download/rstudio-desktop) (version gratuite)

> **Installer RStudio APRÈS R.** L'inverse oblige à refaire la configuration.

## 3. Premier script R

```{r}
#| label: chunk-05
x <- 5
y <- 3
z <- x + y
print(z)

# Le symbole <- assigne une valeur a un objet -- equivalent de generate/gen
# en STATA, ou de compute en SPSS. Le signe = fonctionne aussi la plupart
# du temps, mais <- reste la convention R.

# Bonne pratique (slide 14) : nommer clairement ses objets --------------------
resultat_test <- z * 10          # plutot que "a" ou "temp"
resultat_test                    # un objet cree reste en memoire jusqu'a la
                                  # fin de la session : surveiller le panneau
                                  # Environment (haut-droite dans RStudio)
```

## 4. Packages et librairies

Deux mots à ne pas confondre :

- un **package** est un ensemble de fonctions, de données et de documentation —
  comparable à un module SPSS ou à un jeu d'ado-files STATA ;
- une **librairie** est le dossier où les packages sont installés sur votre poste.

```{r}
#| label: chunk-07
# Liste des packages necessaires pour le J01 et pour la suite du parcours
packages_j01 <- c(
  "tidyverse", # coeur de la manipulation : dplyr, ggplot2, tidyr, readr...
  "haven",     # lire les fichiers STATA (.dta) et SPSS (.sav) avec labels
  "readxl",    # lire les fichiers Excel (.xls / .xlsx) sans Excel installe
  "labelled"   # gerer les labels de valeurs/variables a la STATA/SPSS
)

# Installation "intelligente" : n'installe que ce qui manque -----------------
# (equivalent slide 10, colonne "CRAN" -- install.packages() suffit dans
# l'immense majorite des cas)
packages_manquants <- packages_j01[!(packages_j01 %in% installed.packages()[, "Package"])]
if (length(packages_manquants) > 0) {
  install.packages(packages_manquants)
} else {
  message("Tous les packages du J01 sont deja installes.")
}

# Autres sources d'installation possibles (slide 10), a titre indicatif :
#   GitHub       : devtools::install_github("tidyverse/ggplot2")
#   Fichier local: install.packages("chemin/fichier.tar.gz", repos = NULL, type = "source")
#   Interface    : Tools > Install Packages

# Chargement (a faire a CHAQUE session, contrairement a l'installation) ------
library(tidyverse)
library(haven)
library(readxl)
library(labelled)

# --- Conflits de fonctions (masking) -- slide 9 -----------------------------
# Quand deux packages charges partagent une fonction du meme nom, le dernier
# charge masque l'autre. R vous previent avec un message
# "The following objects are masked...". Pour lever toute ambiguite, on
# prefixe explicitement la fonction par son package :
#   dplyr::filter(df, condition)
#   stats::filter(x, ...)
```

## 5. Importer les données de la formation

```{r}
#| label: chunk-08
## Extrait DHS Cameroun 2018, au format SPSS (.sav) :
##   - fichier menages (Household Recode) : CMHR71FL.SAV
##   - fichier femmes  (Individual Recode) : CMIR71FL.SAV
## Les deux fichiers sont dans le sous-dossier datasets/ de CE dossier.
## S'ils manquent : lancer source("outils/distribuer_donnees.R") depuis la
## racine du projet (ouvrir atelier-r-spatial-iford-2026.Rproj).

# Definir le dossier de travail (equivalent de "cd" en STATA) ---------------
# setwd("chemin/vers/le/dossier")   # a decommenter et adapter si besoin
dossier_donnees <- "."              # dossier de CE document ; les chemins
                                    # ci-dessous lui sont relatifs
```

### 5.1 — Fichier ménages : SPSS (`.sav`), labels conservés

```{r}
#| label: chunk-09
chemin_menages <- file.path(dossier_donnees, "datasets/CMHR71FL.SAV")
if (file.exists(chemin_menages)) {
  df_menages <- read_sav(chemin_menages)
  message("Fichier menages (.sav) importe : ", nrow(df_menages), " lignes, ",
          ncol(df_menages), " variables.")
} else {
  message("[Info] '", chemin_menages, "' introuvable -- lancez ",
          "source(\"outils/distribuer_donnees.R\") puis reexecutez.")
  df_menages <- NULL
}
```

### 5.2 — Fichier femmes : SPSS (`.sav`) également

```{r}
#| label: chunk-10
chemin_femmes <- file.path(dossier_donnees, "datasets/CMIR71FL.SAV")
if (file.exists(chemin_femmes)) {
  df_femmes <- read_sav(chemin_femmes)
  message("Fichier femmes (.sav) importe : ", nrow(df_femmes), " lignes, ",
          ncol(df_femmes), " variables.")
} else {
  message("[Info] '", chemin_femmes, "' introuvable -- lancez ",
          "source(\"outils/distribuer_donnees.R\") puis reexecutez.")
  df_femmes <- NULL
}
```

### 5.3 — Les autres formats : `.dta` (STATA), `.csv`, Excel

```{r}
#| label: chunk-11
#| eval: false
# Le geste est identique quel que soit le format -- seule la fonction change.
# (Bloc montre a titre de reference : ces fichiers ne sont pas fournis ce jour.)

df <- haven::read_dta("chemin/vers/fichier.dta")   # STATA -- garde les labels
df <- haven::read_sav("chemin/vers/fichier.sav")   # SPSS  -- garde les labels
df <- readr::read_csv("chemin/vers/fichier.csv")   # CSV   -- pas de labels
df <- readxl::read_excel("chemin/vers/fichier.xlsx")  # Excel, feuille 1 par defaut
```

```{r}
#| label: chunk-11b
# --- Pourquoi haven ? --------------------------------------------------------
# Concu specifiquement pour les utilisateur.rice.s de STATA et SPSS : il
# importe les fichiers .dta/.sav en conservant les labels de valeurs et de
# variables, pour retrouver des reperes familiers dans R.
# --- PIEGE DE NOMMAGE, a connaitre --------------------------------------------
# DHS nomme ses variables en MINUSCULES dans les fichiers STATA (.dta) mais en
# MAJUSCULES dans les fichiers SPSS (.sav). Un test "v201" %in% names(df)
# echoue donc silencieusement sur un .sav. On resout les noms sans tenir
# compte de la casse, avec cette petite fonction reutilisee plus bas :
trouver_vars <- function(df, vars) {
  correspondance <- match(toupper(vars), toupper(names(df)))
  names(df)[correspondance[!is.na(correspondance)]]
}

# Verifions les labels sur la variable "milieu de residence" du fichier menages :
var_milieu <- trouver_vars(df_menages, "hv025")
if (!is.null(df_menages) && length(var_milieu)) {
  cat("Variable trouvee sous le nom :", var_milieu, "\n")
  cat("Label de la variable :", attr(df_menages[[var_milieu]], "label"), "\n\n")
  print(labelled::val_labels(df_menages[[var_milieu]]))  # equiv. : label list
}
```

## 6. Premiers réflexes après l'import

```{r}
#| label: chunk-12
## Les memes reflexes qu'en STATA/SPSS, transposes en R -- a executer sur
## chaque nouveau jeu de donnees, en debut d'exploration.

## ATTENTION : un fichier DHS complet compte plusieurs MILLIERS de variables.
## str() ou summary() sur l'objet entier produisent des sorties illisibles.
## On explore donc un SOUS-ENSEMBLE de variables -- reflexe a garder en
## pratique reelle (equivalent : describe var1 var2 ... plutot que describe).

## On reutilise trouver_vars() definie en section 5.3 : elle resout les noms
## de variables sans tenir compte de la casse (.sav en MAJUSCULES vs .dta en
## minuscules chez DHS).

explorer_jeu_de_donnees <- function(df, nom = "df", vars = NULL) {
  # Fonction utilitaire : regroupe les 5 reflexes de la slide 13,
  # pour ne pas avoir a les retaper a chaque import.
  if (is.null(df)) {
    message("[", nom, "] jeu de donnees non charge -- rien a explorer.")
    return(invisible(NULL))
  }
  cat("\n==== Dimensions (", nom, ") ====\n", sep = "")
  cat(nrow(df), "lignes x", ncol(df), "variables\n")

  # On restreint aux variables demandees et reellement presentes
  vars <- trouver_vars(df, vars)
  if (!length(vars)) {
    warning("Aucune des variables demandees n'existe dans ", nom,
            " -- repli sur les 10 premieres colonnes.")
    vars <- names(df)[seq_len(min(10, ncol(df)))]
  }
  sous_df <- df[, vars, drop = FALSE]
  cat("Exploration sur", length(vars), "variables :",
      paste(vars, collapse = ", "), "\n")

  cat("\n==== Structure -- equiv. describe/codebook ====\n")
  str(sous_df)                        # equivalent : describe / codebook
  cat("\n==== Premieres lignes -- equiv. browse/Data View ====\n")
  print(head(sous_df))                # equivalent : browse / Data View
  cat("\n==== Statistiques descriptives -- equiv. summarize ====\n")
  print(summary(sous_df))             # equivalent : summarize
  cat("\n==== Valeurs manquantes -- equiv. misstable summarize ====\n")
  print(colSums(is.na(sous_df)))      # equivalent : misstable summarize
}

# Variables de demonstration (codes standard DHS) ----------------------------
#   menages (HR) : hv001 grappe, hv002 numero menage, hv009 taille,
#                  hv024 region, hv025 milieu, hv206 electricite, hv270 quintile
#   femmes  (IR) : v012 age, v024 region, v025 milieu, v106 instruction,
#                  v201 nb enfants nes vivants, v190 quintile
vars_menages <- c("hv001", "hv002", "hv009", "hv024", "hv025", "hv206", "hv270")
vars_femmes  <- c("v012", "v024", "v025", "v106", "v201", "v190")

explorer_jeu_de_donnees(df_menages, "df_menages", vars_menages)
explorer_jeu_de_donnees(df_femmes,  "df_femmes",  vars_femmes)

# Tri a plat d'une variable precise (equivalent : tab varname) --------------
var_enfants <- trouver_vars(df_femmes, "v201")
if (!is.null(df_femmes) && length(var_enfants)) {
  cat("\n==== Nombre d'enfants nes vivants (", var_enfants,
      ") -- equiv. tab v201 ====\n", sep = "")
  print(table(df_femmes[[var_enfants]], useNA = "ifany"))
} else {
  message("[Info] Variable 'v201' absente de df_femmes -- adaptez le nom ",
          "de variable a votre extrait avant de relancer table().")
}

# Nombre total de variables disponibles (la liste complete est trop longue
# pour etre affichee : on montre comment y acceder) -------------------------
if (!is.null(df_menages)) {
  cat("\ndf_menages :", ncol(df_menages), "variables. 10 premieres :\n")
  print(head(names(df_menages), 10))
  # Liste complete : names(df_menages)  --  ou View(names(df_menages))
}
if (!is.null(df_femmes)) {
  cat("\ndf_femmes :", ncol(df_femmes), "variables. 10 premieres :\n")
  print(head(names(df_femmes), 10))
}
```

## 7. Bonnes pratiques dès le départ

Quatre réflexes à installer aujourd'hui — ils vous suivront jusqu'au J11.

1. **Travailler par projet RStudio** (*File > New Project*). Un projet associe un
   dossier de travail au script et évite les chemins absolus, qui cassent dès
   qu'on change d'ordinateur.
2. **Garder le script comme trace complète.** Comme un do-file, il doit permettre
   de tout refaire de zéro — `install.packages()` et `library()` inclus en haut
   de fichier (voir section 4 ci-dessus).
3. **Commenter au fil de l'eau.** Le symbole `#` commente une ligne, exactement
   comme `*` ou `//` dans STATA.
4. **Nommer clairement les objets** — `df_menages`, `df_femmes`… plutôt que
   `df1`, `df2`, `temp`. Et surveiller régulièrement le panneau *Environment*.

## 8. Mini-exercice guidé — à vous de jouer

Objectif : reproduire les cinq compétences du J01 (slide 2) sur un objet simple,
sans dépendre des fichiers DHS.

### 8.1 — Scripter : créer trois objets et un calcul simple

```{r}
#| label: chunk-18
poids_kg   <- 62
taille_m   <- 1.68
imc        <- poids_kg / (taille_m^2)
imc
```

### 8.2 — Transposer : recréer un « tri à plat » comme en STATA/SPSS

```{r}
#| label: chunk-19
# vecteur factice representant une variable categorielle
milieu_residence <- c("Urbain", "Rural", "Urbain", "Urbain", "Rural")
table(milieu_residence)                  # equivalent : tab milieu_residence
```

### 8.3 — S'orienter : vérifier ce qui se trouve dans l'Environment

```{r}
#| label: chunk-20
ls()                                     # liste des objets crees dans la session
```

### 8.4 — Consigne pour la salle

- Dupliquer les blocs 8.1 à 8.3 juste en dessous
- Remplacer les valeurs par les vôtres
- Exécuter avec `Ctrl+Entrée` ligne par ligne, puis vérifier le résultat dans la
  Console et dans le panneau *Environment*

## 9. Checklist de fin de journée

- [ ] R et RStudio installés et ouverts sans erreur
- [ ] Ce script exécuté du début à la fin sans erreur bloquante
- [ ] `tidyverse`, `haven`, `readxl` et `labelled` installés et chargés
- [ ] Le jeu de données DHS Cameroun 2018 importé avec succès
      (`df_menages` / `df_femmes` non `NULL`)
- [ ] `str()`, `summary()`, `table()`, `colSums(is.na())` exécutés sur un
      sous-ensemble de variables
- [ ] Un projet RStudio créé pour la suite de la formation

> **En cas de blocage à l'installation**, une session de rattrapage est prévue en
> début de J02 plutôt que de retarder le groupe complet.

## 10. Feuille de route — la suite du parcours

Les onze journées de l'atelier, pour situer celle-ci dans l'ensemble :

| Jour | Thème |
|:----:|:------|
| **J01** | **Fondations R & RStudio** — *aujourd'hui* |
| J02 | Penser l'espace : les données géospatiales *(+ module « Bien utiliser l'IA »)* |
| J03 | L'univers vectoriel : points, lignes, polygones avec `sf` |
| J04 | La Terre en pixels : les données raster avec `terra` |
| J05 | Des enquêtes à la carte : géolocaliser les données d'enquête |
| J06 | L'art de la visualisation cartographique |
| J07 | Statistiques spatiales : autocorrélation, voisinage, agrégats |
| J08 | Compter chaque habitant : la population en haute résolution |
| J09 | Voir le territoire depuis l'espace : la télédétection |
| J10 | Les données spatiales au service des politiques publiques |
| J11 | Pérenniser et transmettre |

Les trois journées qui suivent immédiatement :

- **J02 — Penser l'espace.** Une journée *sans R* : ce qu'est une coordonnée, un
  système de référence, une projection ; vecteur contre raster ; où trouver les
  données. L'après-midi est consacrée au module « Bien utiliser l'IA pour
  l'analyse de données ».
- **J03 — L'univers vectoriel.** Le package `sf` : lire un shapefile, comprendre
  la colonne `geometry`, jointures attributaires et spatiales, premières cartes
  avec `ggplot2` et `geom_sf()`.
- **J04 — La Terre en pixels.** Le package `terra` : lire un raster, résolution
  et emprise, algèbre de bandes, extraction de valeurs vers des polygones.

```{r}
#| label: chunk-23
## Les gestes acquis aujourd'hui (import, str, summary, table, colSums(is.na))
## restent le point de depart de CHAQUE journee : avant de cartographier une
## donnee, on l'importe et on l'inspecte. La couche spatiale s'ajoute a ces
## reflexes, elle ne les remplace pas.
```
