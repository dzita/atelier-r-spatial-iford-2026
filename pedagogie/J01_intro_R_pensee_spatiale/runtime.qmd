---
title: "J1 — Intro R + pensée spatiale"
subtitle: "Code R exécutable dans le navigateur"
engine: knitr
---

::: {.iford-callout}
**Mode d'emploi.** Cliquez **Run code** dans chaque cellule pour exécuter. Modifiez le code librement, recliquez pour relancer. Rien n'est envoyé sur un serveur — tout tourne dans votre navigateur.
:::

## 1. R comme calculatrice

```{webr}
#| autorun: true
# Les 10 régions du Cameroun et leur population estimée
regions <- c("Adamaoua", "Centre", "Est", "Extrême-Nord", "Littoral",
             "Nord", "Nord-Ouest", "Ouest", "Sud", "Sud-Ouest")
pop_est <- c(1340000, 4400000, 870000, 4200000, 3400000,
             2700000, 2000000, 2000000, 800000, 1700000)

length(regions)
sum(pop_est)
mean(pop_est)
```

**À essayer :** changez les valeurs de `pop_est`, ou ajoutez une 11ᵉ région imaginaire — puis relancez.

## 2. Vecteurs et opérations vectorielles

```{webr}
# Conversion en millions
pop_millions <- pop_est / 1e6
round(pop_millions, 2)

# Régions de plus de 3 millions
regions[pop_est > 3e6]
```

## 3. Tibble : la table de données moderne

```{webr}
library(tibble)
library(dplyr)

tbl_cmr <- tibble(
  region   = regions,
  pop_2019 = pop_est,
  capitale = c("Ngaoundéré", "Yaoundé", "Bertoua", "Maroua", "Douala",
               "Garoua", "Bamenda", "Bafoussam", "Ebolowa", "Buea")
)

tbl_cmr
```

## 4. Les verbes dplyr en pratique démographique

```{webr}
tbl_cmr |>
  mutate(
    pop_millions = pop_2019 / 1e6,
    classe = case_when(
      pop_2019 < 1e6 ~ "Moins de 1M",
      pop_2019 < 3e6 ~ "1 à 3M",
      TRUE           ~ "Plus de 3M"
    )
  ) |>
  arrange(desc(pop_2019))
```

```{webr}
tbl_cmr |>
  filter(pop_2019 > 3e6) |>
  select(region, capitale, pop_2019)
```

## 5. Un mini-microfichier d'enquête simulé

```{webr}
set.seed(2026)
n <- 5000
hh <- tibble(
  region      = sample(regions, n, replace = TRUE, prob = pop_est / sum(pop_est)),
  milieu      = sample(c("Urbain","Rural"), n, replace = TRUE, prob = c(0.57, 0.43)),
  taille_mng  = pmax(1, round(rpois(n, lambda = 5.1))),
  electricite = sample(c("Oui","Non"), n, replace = TRUE, prob = c(0.61, 0.39))
)
head(hh)
```

```{webr}
hh |>
  group_by(region) |>
  summarise(
    n               = n(),
    taille_moyenne  = round(mean(taille_mng), 2),
    pct_urbain      = round(mean(milieu == "Urbain") * 100, 1),
    pct_electricite = round(mean(electricite == "Oui") * 100, 1)
  ) |>
  arrange(desc(pct_electricite))
```

::: {.takeaway}
**À retenir.** `filter` filtre les lignes, `select` choisit les colonnes, `mutate` crée une variable, `group_by + summarise` agrège. Quatre verbes pour 80 % de l'analyse de données.
:::

## 6. Première carte — vraies limites GADM Cameroun

Les fichiers GADM v4.1 du Cameroun sont servis par le même site que ce runtime. On les télécharge depuis le navigateur dans la machine virtuelle de WebR avec `download.file()`, puis on les lit avec `sf::read_sf()`. Premier téléchargement ~1 à 2 secondes selon le wifi, ensuite c'est en cache.

```{webr}
#| autorun: true
library(sf)
library(ggplot2)
library(dplyr)

# Telecharge GADM v4.1 ADM1 (10 regions, ~190 Ko)
download.file("/_commons/data/gadm41_CMR_1.json", "adm1.json", mode = "wb")
adm1 <- read_sf("adm1.json")

# Inspection rapide
nrow(adm1)
st_crs(adm1)$epsg
head(adm1$NAME_1)
```

Et maintenant la vraie carte des 10 régions du Cameroun, avec les contours administratifs officiels :

```{webr}
ggplot(adm1) +
  geom_sf(aes(fill = NAME_1), color = "white", linewidth = 0.3, show.legend = FALSE) +
  geom_sf_label(aes(label = NAME_1),
                size = 2.4, color = "white", fontface = "bold",
                label.padding = unit(0.1, "lines")) +
  scale_fill_viridis_d(option = "G") +
  labs(title = "Les 10 régions du Cameroun",
       subtitle = "Limites administratives ADM1 — GADM v4.1",
       caption = "Source : GADM v4.1 · Atelier IFORD × GDSG 2026") +
  theme_minimal()
```

::: {.iford-callout}
**À essayer :** charge maintenant ADM2 (départements) avec `download.file("/_commons/data/gadm41_CMR_2.json", "adm2.json")` puis trace une carte par département. Tu verras un découpage 5 à 10 fois plus fin.
:::

## 6 bis. Choroplèthe densité — carte thématique complète

On combine la géométrie ADM1 avec le tibble de population créé en section 3, on calcule la densité (habitants par km²), et on colore.

```{webr}
# Reprojection en UTM 32N pour mesurer correctement les superficies
adm1_utm <- st_transform(adm1, 32632)
adm1_utm$superficie_km2 <- as.numeric(st_area(adm1_utm)) / 1e6

# Verification : superficie totale ~475 000 km2 (CMR officiel)
sum(adm1_utm$superficie_km2) |> round()
```

```{webr}
# Jointure attributaire avec la table population
pop_tbl <- tibble::tibble(NAME_1 = regions, pop_2019 = pop_est)

adm1_pop <- adm1_utm |>
  left_join(pop_tbl, by = "NAME_1") |>
  mutate(densite = pop_2019 / superficie_km2)

# Tableau trie par densite decroissante
adm1_pop |>
  st_drop_geometry() |>
  select(NAME_1, pop_2019, superficie_km2, densite) |>
  arrange(desc(densite))
```

```{webr}
ggplot(adm1_pop) +
  geom_sf(aes(fill = densite), color = "white", linewidth = 0.3) +
  geom_sf_label(aes(label = NAME_1),
                size = 2.2, color = "white", fontface = "bold",
                label.padding = unit(0.08, "lines")) +
  scale_fill_viridis_c(option = "B",
                       name = "Densité\n(hab/km²)",
                       trans = "sqrt") +
  labs(title = "Densité de population par région — Cameroun (estim. 2019)",
       caption = "Source : BUCREP (estimations), GADM v4.1 · IFORD × GDSG 2026") +
  theme_minimal()
```

## 7. Distance entre Yaoundé et Douala — démo CRS

```{webr}
library(sf)

yaounde <- st_sfc(st_point(c(11.5021, 3.8480)), crs = 4326)
douala  <- st_sfc(st_point(c(9.7679,  4.0511)), crs = 4326)

# Distance correcte (en mètres) — st_distance gère la sphère
dist_m <- st_distance(yaounde, douala)
dist_m

# Comparaison avec une distance "naïve" sur les degrés (FAUSSE)
cat("Distance naïve (degrés - sans dimension):",
    round(sqrt((11.5021-9.7679)^2 + (3.8480-4.0511)^2), 4), "\n")
cat("Distance correcte (km):", round(as.numeric(dist_m) / 1000, 1), "km\n")
```

::: {.iford-callout}
La distance Yaoundé–Douala sur la route est d'environ **240 km**. Le calcul « naïf » sur les degrés donne un nombre sans dimension — vérifiez par vous-même qu'il ne ressemble en rien aux 240 km attendus.
:::

## 8. Pour aller plus loin

Ce runtime couvre les bases manipulables dans le navigateur. La **démo complète du jour** (avec `tmap` pour les cartes thématiques publiables, exports PNG/PDF haute résolution, et le pipeline de chargement via `fetch_data.R`) est dans **`pedagogie/J01_intro_R_pensee_spatiale/demo.qmd`** — à ouvrir dans RStudio ou Posit Cloud avec R installé localement.

Le **devoir individuel Q6** (cartographier votre région d'origine) est dans `exercice.qmd` ; le corrigé pédagogique est dans `corrige.qmd`.

[← Retour à l'accueil](../INDEX.md)
